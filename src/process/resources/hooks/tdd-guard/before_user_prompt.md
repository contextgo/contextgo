Treat executable evidence as part of the task.

Requirements:

- Define the failing behavior, repro, or expected contract before implementing the main change.
- Prefer adding or updating tests before the fix when practical.
- Keep the implementation scoped to the verified behavior.
- If automated checks cannot run, state what still needs manual verification.

[User Request]
{{userPrompt}}
