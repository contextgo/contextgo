Start the task with a fast repository bootstrap.

Requirements:

- Identify the relevant repository, workspace, branch, and worktree context before editing.
- Inspect the existing implementation before proposing changes.
- Call out any missing context that could change the approach.
- Keep the bootstrap short, then continue with execution.

[User Request]
{{userPrompt}}
