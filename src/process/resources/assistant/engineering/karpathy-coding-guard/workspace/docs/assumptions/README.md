# Assumptions

Use this folder to record:

- what is known
- what is inferred
- what still needs confirmation before implementation

If a task has multiple plausible interpretations, write them down here before coding instead of choosing silently.
