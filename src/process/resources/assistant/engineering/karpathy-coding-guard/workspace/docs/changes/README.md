# Change Boundaries

Use this folder to record:

- which files or modules are in scope
- which nearby areas are explicitly out of scope
- what would count as an unnecessary expansion of the diff

This package assumes that a smaller correct change is better than a broader cleanup pass.
