# Workspace Instructions

This workspace was initialized for ContextGo's built-in **Karpathy Coding Guard** assistant.

Use this file as the repository entry point for package-specific guidance. Keep it short and route detailed task material into `docs/`.

## Workspace Model

- `.contextgo/` is the installed workspace state for this package.
- Runtime-native skill directories are projections only.
- `docs/` is where assumptions, scope boundaries, and verification notes should live.

## Context Routing

- Read `docs/README.md` for the workspace documentation map.
- Read `docs/assumptions/README.md` before coding when the request is ambiguous.
- Read `docs/changes/README.md` to record the allowed change boundary and non-goals.
- Read `docs/verification/README.md` to capture success criteria and validation evidence.

## Project-Specific Instructions

Fill in:

- the repository areas this assistant is allowed to change by default
- what kinds of ambiguity must be confirmed before coding
- how to record verification commands and results for this project
