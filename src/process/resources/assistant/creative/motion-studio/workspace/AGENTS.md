# Workspace Instructions

This workspace was initialized for ContextGo's built-in **Motion Studio** assistant.

Use this file as the motion entry point for workspace guidance. Keep it concise and route detailed motion work into `docs/`.

## Workspace Model

- `.contextgo/` is the installed workspace state for this package.
- Runtime-native skill directories are projections only.
- `docs/` is a progressive-disclosure context surface for storyboards, scenes, renders, and QC.

## Context Routing

- Read `docs/README.md` for the workspace documentation map.
- Read `docs/storyboards/README.md` for storyboard sources of truth.
- Read `docs/scenes/README.md` for scene recipes used in this workspace.
- Read `docs/renders/README.md` for render manifests, configs, and contact sheets.
- Read `docs/qc/README.md` for QC reports, approvals, and rerun decisions.

## Project-Specific Instructions

Fill in:

- the channels and aspect ratios this project regularly produces
- the brand or visual constraints that apply to every motion artifact
- where storyboards, render outputs, and QC reports should live in this repository
- which motion runtime is expected, and any environment requirements
