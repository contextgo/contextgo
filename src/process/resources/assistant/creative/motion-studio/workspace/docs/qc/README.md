# QC

Each QC report lives under `qc/<storyboardId>/<targetId>/report.md` and follows the package review checklist.

Reports should record:

- which review passes succeeded
- which passes flagged issues
- which scenes need a rerun and what changed expectation should be
- the final status: approved, blocked, or minor edits needed
