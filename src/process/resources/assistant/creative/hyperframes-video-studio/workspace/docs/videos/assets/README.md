# Video Assets

Store source assets here: screenshots, generated images, generated video snippets, audio, subtitle files, fonts, charts, and data files.

Prefer local assets over remote URLs so HyperFrames renders are repeatable.
