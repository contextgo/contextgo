# Render Manifests

Store one manifest per rendered output here.

Manifests should record project path, render command, output path, dimensions, duration, fps, source assets, generated media lineage, and QC status.
