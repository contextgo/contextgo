# Workspace Instructions

This workspace was initialized for ContextGo's built-in **Visual Artifact
Runner** assistant.

Use this file as the visual artifact entry point for workspace guidance. Keep
it concise and route detailed work into `docs/`.

## Workspace Model

- `.contextgo/` is the installed workspace state for this package.
- Runtime-native skill directories are projections only.
- `docs/` is a progressive-disclosure context surface for visual artifact work.

## Context Routing

- Read `docs/README.md` for the workspace documentation map.
- Read `docs/inputs/README.md` for normalized brief, report, PDF, and data input
  conventions.
- Read `docs/recipes/README.md` for the layout recipes available in this
  workspace.
- Read `docs/exports/README.md` for the supported export modes and where build
  outputs land.

## Project-Specific Instructions

Fill in:

- the audiences and surfaces this workspace produces artifacts for
- which themes and brand constraints the artifacts must respect
- where build outputs, build notes, and QC reports should be written
- whether `morph-ppt` is allowed as a deck execution backend in this workspace
