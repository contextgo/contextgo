# Recipes

Use this document space when documenting which layout recipes are in active
use for this workspace and any project-specific overrides.

Capture:

- the deck, PDF, and infographic recipes the team prefers
- any project-specific constraints layered on top of the canonical recipe
- the rationale for choosing one recipe over another for repeated artifact
  types
- the recipe versions in use, so QC sweeps can detect drift
