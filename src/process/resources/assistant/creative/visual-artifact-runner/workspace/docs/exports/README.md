# Exports

Use this document space when documenting the export modes used in this
workspace and where build outputs land.

Capture:

- the default export mode for each artifact type produced by the team
- whether `pptx-morph` is allowed as an export mode in this workspace
- the build output directory layout (artifact files, build notes, asset
  inventories, failure lists)
- any post-export distribution steps owned by the team
